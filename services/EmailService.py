class EmailService:

    @staticmethod
    def send_message(user_id, message):
        # Placeholder for sending in-app message logic
        print(f"Sending in-app message to user {user_id}: {message}")

    @staticmethod
    def send_email(email, subject, body):
        # Placeholder for sending email logic
        print(f"Sending email to {email} with subject '{subject}': {body}")